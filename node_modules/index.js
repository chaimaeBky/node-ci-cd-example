const express = require('express');
const app = express();

app.get('/', (req, res) => {
  res.send('Hello, CI/CD!');
});

const port = 3000;
app.listen(port, () => {
    //ci
  console.log(`Server running on http://localhost:${port}`);
});
